// Package core defines what Agenterr tracks: logs, the events among them,
// and the issues those events group into. It imports only the standard
// library — everything else in Agenterr depends on core, never the reverse.
package core
